#include <stdio.h>

/**
 * main - creates  an infinite loop
 * Return: 0
 */

int main(void)
{
	int h;

	printf("Infinite loop incoming :(\n");

	h = 0;
	/*
	 * while (h < 10)
	 * {
	 *	putchar(h);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");

	return (0);
}

